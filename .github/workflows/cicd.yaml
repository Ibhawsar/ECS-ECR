# name: CI-CD Pipeline

# on:
#   push:
#     branches: ["main"]
    
# jobs:
#   build-test-deploy:
#     runs-on: ubuntu-latest
   
#     permissions:
#       id-token: write
#       contents: read

#     env:
      
#       AWS_REGION: us-east-1
#       ECR_REPOSITORY: myapp
#       IMAGE_TAG: ${{ github.sha }}

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3
#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: "3.10"
#       - name: Install dependencies
#         run: |
#           cd app
#           pip install -r requirements.txt
#           pip install pytest
#       - name: Run pytest
#         run: |
#           cd app
#           pytest


#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: arn:aws:iam::161862142232:role/GirRole
#           aws-region: us-east-1

#       - name: Login to Amazon ECR
#         uses: aws-actions/amazon-ecr-login@v2


#       - name: Build, tag, and push image to ECR
#         run: |
#           docker build -t $ECR_REPOSITORY:$IMAGE_TAG .
#           docker tag "$ECR_REPOSITORY:$IMAGE_TAG" "$ECR_REPOSITORY:$IMAGE_TAG"
#           docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG


#       - name: Deploy to Amazon ECS
#         uses: aws-actions/amazon-ecs-deploy-task-definition@v2
#         with:
#           task-definition: terraform/ecs-task.json
#           service: fastapi-gists-service
#           cluster: fastapi-gists-cluster
#           wait-for-service-stability: true

name: CI/CD Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  id-token: write
  contents: read

env:
  IMAGE_TAG: ${{ github.sha }}

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

     
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          cd app
          pip install -r requirements.txt
          pip install pytest

      - name: Run tests
        run: |
          cd app
          pytest -q

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v3
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME_DEPLOY }}
          aws-region: ${{ secrets.AWS_REGION }}


      - name: Login to ECR
        run: |
          aws ecr get-login-password --region ${{ secrets.AWS_REGION }} \
            | docker login --username AWS --password-stdin \
              ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com


      - name: Build and Push Docker image
        env:
          AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
          IMAGE_TAG: ${{ github.sha }}
        run: |

          IMAGE_SHA="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPOSITORY}:${IMAGE_TAG}"
          IMAGE_LATEST="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPOSITORY}:latest"

          echo "Building images: $IMAGE_SHA and $IMAGE_LATEST"

          docker build -t "$IMAGE_SHA" -t "$IMAGE_LATEST" .

          docker push "$IMAGE_SHA"
          docker push "$IMAGE_LATEST"

      - name: Deploy to Amazon ECS
        env:
          CLUSTER: ${{ secrets.ECS_CLUSTER_NAME }}
          SERVICE: ${{ secrets.ECS_SERVICE_NAME }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
        run: |
          echo "Deploying new version to ECS..."
          aws ecs update-service \
            --cluster "$CLUSTER" \
            --service "$SERVICE" \
            --force-new-deployment

